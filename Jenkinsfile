pipeline { 
    agent any
    stages {
        stage("Using curl example") {
            steps {
                sh '''
                  env
                  ls -l
                  '''
            }
}
    }
}
