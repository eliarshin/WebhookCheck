pipeline { 
    agent anyhrehre
    stages {
        stage("Using curl example") {
            steps {
                sh '''
                  env
                  ls
                  '''
            }
}
    }
}
