pipeline { 
    agent any
    stages {
        stage("Using curl example") {
            steps {54543534
                sh '''
                  env
                  ls
                  '''
            }
}
    }
}
