pipeline { 
    agent any
    stages {
        stage("Using curl example") {
            steps {
                sh '''
                  env
                  ls -la
                  ls -l
                  ls -la
                  ls -l
                  '''
            }
}
    }
}
